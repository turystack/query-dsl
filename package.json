{
	"name": "@turystack/query-dsl",
	"description": "Zod schemas and serializers for type-safe HTTP query parameters.",
	"private": false,
	"version": "1.0.2",
	"type": "module",
	"sideEffects": false,
	"main": "./dist/index.js",
	"module": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js",
			"default": "./dist/index.js"
		}
	},
	"publishConfig": {
		"access": "public"
	},
	"engines": {
		"node": ">=20"
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:cov": "vitest run --coverage",
		"lint": "biome lint ./src",
		"format": "biome format --write ./src",
		"check": "biome check ./src",
		"check:fix": "biome check --write ./src",
		"prepack": "pnpm run build"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/turystack/query-dsl.git"
	},
	"peerDependencies": {
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.4",
		"@vitest/coverage-v8": "^4.1.10",
		"reflect-metadata": "^0.2.2",
		"tsc-alias": "^1.9.1",
		"typescript": "^7.0.2",
		"vite": "^8.1.5",
		"vitest": "^4.1.10",
		"zod": "^4.4.3"
	}
}
